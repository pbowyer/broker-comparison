{
  "name": "broker-costs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/fee-engine.js && node --check src/persistence.js && node --check src/presentation.js && node --check src/providers.js && node --check src/state.js && node --check scripts/build-static.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
